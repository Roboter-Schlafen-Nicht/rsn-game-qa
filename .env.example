# RSN Game QA — Environment Variables
#
# Copy this file to .env and fill in your values.
# The .env file is gitignored and never committed.
#
# All scripts use python-dotenv to load these automatically.
# You can also set them as real environment variables (CI, Docker, etc.).

# ── Roboflow (image annotation platform) ─────────────────────────────
ROBOFLOW_API_KEY=your_api_key_here
ROBOFLOW_WORKSPACE=your_workspace_slug
ROBOFLOW_PROJECT=breakout71
